import React from 'react';
import {useNavigate} from "react-router-dom";

const DelCertInfo = () => {
    const navigate = useNavigate();
    return (
        <div>
            인증서 삭제 화면 입니다.
        </div>
    );
};

export default DelCertInfo;