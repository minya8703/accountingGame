import React from 'react';
import {useNavigate} from "react-router-dom";

const UptCertInfo = () => {
    const navigate = useNavigate();
    return (
        <div>
            인증서변경 화면 입니다.
        </div>
    );
};

export default UptCertInfo;